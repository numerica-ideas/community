apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-backend.fullname" . }}-test-connection"
  labels:
    {{- include "spring-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spring-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
